<!-- <% @title = "#{@project.name} - Project - Ticketee" %> -->
<% title(@project.name, "Projects") %>

<%= @project.name %>
<%= @project.description %>
<%= link_to "Edit Project", edit_project_path(@project.id ) %>
<%= link_to "Delete Project",
            project_path(@project.id),
            method: :delete,
            data: { confirm: "Are you sure want to delete project?" } %>

<h1>Index View</h1>

<% if @messages %>
  <% @messages.each do |message|  %>
    <!-- <h3><%= link_to message.title, message_path(message.id) %></h3> -->
    <h3><%= message.title %></h3>
    <%= link_to "View message", message_path(message.id), :class => "btn btn-default" %>
  <% end %>

<% end %>


<hr  />
<%= link_to "New Message", new_message_path, "class": "btn btn-primary" %>
